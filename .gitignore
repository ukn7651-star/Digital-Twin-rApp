# Run artifacts
output/
oai_run/
*.log
kpis.csv

# OAI source/build lives outside the repo (installed via oai/setup_oai.sh)
openairinterface5g/

# Python
__pycache__/
*.pyc
.venv/
experiments/results/scene/

# LaTeX build artifacts (main.pdf is tracked)
paper/*.aux
paper/*.bbl
paper/*.blg
paper/*.out
paper/overleaf_upload.zip

# Pre-filing invention disclosure. This repository is PUBLIC: committing these
# would be a public disclosure and can bar patentability in jurisdictions with
# absolute novelty (EP, CN, JP). Keep local until the patent is filed.
patent_a1/
patent_a1.zip
